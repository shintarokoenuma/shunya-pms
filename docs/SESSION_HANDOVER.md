# shunya-pms 引き継ぎメモ（2026-09-10 設計作業分 / 締め commit 2026-09-11 JST）

★本メモにしかない識別文字列: HANDOVER-2026-09-10-CLOSE-L

## ⓪ 次セッションの最初の一手（これより先に何もしない）

1. スキル `shunya-session-start` を発動する
2. 前回の締めが実際に保存されたかを確認する（`file-write-verification` 鉄則12）

       cd ~/shunya-production-system
       git log --oneline -3 -- docs/SESSION_HANDOVER.md
       grep -c '^## ' docs/SESSION_HANDOVER.md
       grep -c 'HANDOVER-2026-09-10-CLOSE-L' docs/SESSION_HANDOVER.md

   3つ目が 0 なら、このメモは保存されていない（★正常時は 2。冒頭の識別子と、この grep コマンド例の2箇所に出るため）。
3. `date` を1行流して日付を照合する（★本セッションは日をまたいだ。⑫参照）

## ① プロジェクト棲み分け（混同禁止）

- 本件は **shunya-pms**（`~/shunya-production-system` / GitHub `shintarokoenuma/shunya-pms`）
- saagara-v2 / earnpulse / swtras-showroom は**別物**。コマンドを流す前に `pwd` を見る

## ② git の実態（2026-09-11 17:02 JST 実測 + 本メモの commit）

- ブランチ **main**（★feature に戻していない。`shunya-session-close` の方針どおり）
- 本メモ commit 直前の HEAD: **0bb5ad1**
- 未 push なし・untracked なし・**open PR ゼロ件**
- 本セッションの commit（すべて docs のみ。src は1行も触っていない）

| SHA | 内容 |
|---|---|
| a23e1ba | B-172 addendum v0.1（140行・RECON-J）＋ B-200 起票 |
| a502dfb | B-172 仕様確認書 **v1.0 初回**（240行）＋ B-201 起票 ＋ B-170 状態を完了に訂正 |
| 3036132 | v1.0 を **M-007 反映版**に更新（**261行**・D-0 用語定義を新設） |
| 0bb5ad1 | `docs/MEMO_INBOX.md` に **M-004〜M-007** を追記（245行 / 7 `### M-` / 122 insertions・0 deletions） |

## ③ 本セッションの成果 ― B-172 の設計が確定した

Q0〜Q6 をすべて確定し、**D-0〜D-10** として仕様確認書 v1.0 に焼いた。

- 正本: `docs/specs/b-172-client-auth-spec-confirmation-v1_0-2026-09-10.md`（3036132・261行・識別子 `RECON-K`）
- これが **v0.1（6c7336f）と addendum v0.1（a23e1ba）を supersede** する。古い2本は参照しない
- ★ファイル名の日付 2026-09-10 は**設計確定日**。commit は 2026-09-11（日またぎ）。本文冒頭に注記済み

### ★最重要の帰結（次セッションはここから読む）

社外 User を **shunya テナントの中**に置く（Q4=案4-A）ため、
**テナント分離（companyId の自動付与）は社外に対して壁にならない。既定拒否の認可関数が唯一の壁になる。**
したがって `role = EXTERNAL` を**最上位の関門**にし、D-5 のホワイトリストが実質のセキュリティ境界になる。
**穴が1つ空くとコストまで届く**（コストが出る面は RECON-J 実測で38ファイル、うち `lib/pdf` が5件）。

### 確定事項の要約

| # | 内容 |
|---|---|
| D-0 | ★用語定義（下記④） |
| D-1 | 認可の器は社外共通。初版の適用先はクライアントのみ。工場・仕入先・外注パタンナーは B-022 |
| D-2 | 社外ユーザーは User を作る。`UserRole.EXTERNAL` を起こし `User → Client` 参照列を新設。アカウントはクライアント企業に1つ |
| D-3 | 所属は shunya テナント内。認可の主キーは **role 軸**（既存の物理削除ガードは tenantType 軸で、社外には使えない） |
| D-4 | 既定拒否＋ホワイトリスト。3系統（withTenantContext / 各ファイルの requireSession / 各 export の await auth() 直書き）が共通で呼ぶ薄い認可関数を1本新設 |
| D-5 | 社外に出す8項目＝先方品番 / 絵型 / 色 / サイズ / 数量 / **売値** / **納期** / **発注締日**。出さない＝コスト・仕入先・工場・利益率・進捗・他クライアント |
| D-6 | オーダーページは1ページ複数品番。`OrderPage.productId` を DROP NOT NULL。1品番1ブロック（品番・絵型・SKUマトリクス） |
| D-7 | `ClientDisplayPattern` A/B/C/D は進捗専用のまま（コストマスキングの器ではない）。実装は B-197 |
| D-8 | 初版は受注入力に必要な最小限。クライアントアクション5つは B-198 |
| D-9 | ★売値の正は **ProductionEstimate の提示1枚単価**。確定見積（QE-2 / B-143）は使わない → **B-171 は B-143 を待たない** |
| D-10 | ★納期＝弊社がクライアントに約束した納期。社内の実納期とは別物。**発注締日も提示する** |

## ④ ★用語定義（M-007・以後の全 spec で拘束）

| 語 | 定義 |
|---|---|
| 売値 | クライアントへ販売する量産価格（社外に出す） |
| コスト | **量産実績原価＝発注後の 材料費＋工賃**（社外に出さない） |
| 納期 | **弊社がクライアントに約束した納期**。これを提示してオーダーをもらう |
| 発注締日 | オーダーの締切日。★明記が必要 |
| 卸値 | ★**使わない。** shunya はクライアントの上代に関与しないため、概念が存在しない |

- サンプルもほぼ同じ構造
- ★過去の記述（M-002 の4・BACKLOG B-171/B-172 の既存定義欄）の「卸値」は**語の誤り**。「売値」に読み替える。BACKLOG には追記で訂正済み（既存定義文は書き換えていない）
- ★**最終請求はコスト（量産実績原価）を基礎にする。提示（見積ベース）と請求（実績ベース）は一致しない前提の業務である**

## ⑤ 次にやること（ライフサイクル位置つき）

ライフサイクルは 6（受注期間・オーダーページ）→ 7（受注確定）。原マイルストーンは **M4（受注MVP）が B-148 PR-2b で到達済み**。本件は入口（6）を開けるための前提。

| 順 | 内容 | ステップ | 本番影響 | 不可逆性 | 規模 |
|---|---|---|---|---|---|
| 1 | v1.0 §4-1 の 1〜4 を確定（下記⑥） | 6 | なし | なし | 小 |
| 2 | B-172 実装ブリーフ | 6 | なし | なし | 中 |
| 3 | schema 変更（ADD COLUMN / DROP NOT NULL）＋認可関数 | 6 | **あり** | ★migration の適用は不可逆 | 大 |
| 4 | B-171 専用オーダーページ本体 | 6 | **あり** | あり | 大 |

★**手前の空きステップはない**（6 の直前までは実装済み）。
★**本番の受注は0件、社外ユーザーは0人。** 先に dev で検証の器を作る必要がある（§4-1 の12）。

## ⑥ ★次に埋めるべき未確定（v1.0 §4-1 の最重要4件）

1. **「最終の請求はコスト（量産実績原価）になる」の意味** ― 実績原価そのまま／＋マージン／売値を改訂、のどれか。**金額に直結するので推測で設計しない**（B-201）
2. **約束納期と社内の実納期を別の列として持つか** ― schema の列数が変わる
3. **発注締日の器** ― `OrderPage.openUntil`（nullable DateTime・実在）を流用できるか。締日経過後の挙動（閲覧のみ／完全に閉じる）も未定
4. **先方向けページの置き場所** ― `(app)` の内／外。`(app)/layout.tsx` は AppShell（社内ナビ）を被せるため、社外にそのまま見せる前提になっていない

残り12件（税表示・相見積もりのどの PE を正とするか・単価改訂時の追従／固定・社内品番の併記・約束納期を誰がいつ設定するか 等）は v1.0 §4-1 を読む。実測の未確定4件は §4-2。

## ⑦ B-番号の増減（ゼロ件も明記）

- **新規 2件**
  - **B-200** 編集競合制御（同セクションロック・原設計 §5.7 由来）
  - **B-201** ★請求の基礎はコスト＝量産実績原価（B-109 では受けられないことを grep で確認した上で新設）
- **状態変更 1件**
  - **B-170 未着手 → 完了**。RECON-K で実装を実証（`ProductColorway.clientColorName` が schema に実在／migration `20260908000000_b170_product_colorway_client_color_name` が存在／src 3ファイル14ヒット。`Product.clientProductCode` は src 59参照）
- **取り下げ 0件**
- **番号未採番の合意 0件**
- BACKLOG は **201行**（末尾 B-199 / B-200 / B-201・最終行の列数5・番号重複なし）

## ⑧ 繰り延べた要件（10件・新規採番ゼロ件）

ゲート0で v1.0 §5 を開いて実測した。**全件が既存 B番号で受領済み＝浮いている要件はゼロ。**

工場・仕入先・外注パタンナーの開放→**B-022** ／ 進捗表示 A/B/C/D→**B-197** ／ クライアントアクション5つ→**B-198** ／ 認証の基本セキュリティ4項目→**B-199** ／ 編集競合制御→**B-200** ／ 請求の基礎＝コスト→**B-201** ／ 専用オーダーページ本体・1ページの品番集合・未登録の警告・約束納期と実納期の分離→**B-171** ／ 先方コードの表記ゆれ吸収・Excel 取込→**B-149** ／ 確定見積 QE-2→**B-143**（★本件の前提ではない）

## ⑨ メモ受信箱（M-001〜M-007）

- ★**正本はプロジェクトナレッジ側の `claude/MEMO_INBOX.md`。** repo の `docs/MEMO_INBOX.md` は同期先（Claude Code から見える側）
- 本セッションで **M-004〜M-007** を両方に保存した（repo は 0bb5ad1・245行・7 `### M-`・122 insertions / **0 deletions**＝既存 M-001〜M-003 は無傷）
- M-004 Q2/Q5 の回答原文 ／ M-005 Q0〜Q6 の確定表 ／ M-006 売値の出所と請求の基礎 ／ **M-007 用語定義（④）**
- ★`claude/MEMO_INBOX.md` の同期記録を挿入する python が、**repo 現物に存在しないアンカー**を期待していたため、Claude Code がその1ステップだけを落として実行した（⑪の教訓1）。同期記録の行は repo 側に無いままでよい

## ⑩ ナレッジ同期の状況（★隠さず書く）

| ファイル | 状態 |
|---|---|
| `claude/b-172-client-auth-spec-addendum-v0_1-2026-09-10.md` | 登録済み（repo と同一本文） |
| `claude/b-172-client-auth-spec-confirmation-v1_0-2026-09-10.md` | ★**登録済み**（2026-09-11 / claude.ai 側の project_write で実施・repo 3036132 と同一本文） |
| `claude/MEMO_INBOX.md` | 正本。M-007 を先頭に配置した最新版 |
| `SESSION_HANDOVER.md` | ★本メモの push 後に差し替えが必要（claude.ai 側で実施する） |
| `BACKLOG.md` | ★**未同期のまま繰り越し。** ナレッジ側は **2026-09-09 スナップショット（196行）**、repo は **201行**。全文同期には repo のダンプが必要 |

★**次セッションで BACKLOG のナレッジ版を見るときは、196行の古い版である**ことを前提にする。B-197〜B-201 はナレッジ側に無い。

## ⑪ 本セッションの失敗と教訓（再発防止）

1. ★**repo 現物に無いアンカーを python の期待値にした。** アンカーをナレッジ側の正本から書き写した。Claude Code が 0 ヒットで停止し、現物を確認してその1ステップだけを落として完遂した。**誤っていたのは指示側。** → アンカーは同じブロックの STEP 0 で grep して1件であることを確認する
2. ★**約330行の全文上書きブロックが届かなかった。** `cat >>` の追記（約150行）に切り替えたら一度で通った。検証は `git diff --numstat` の削除0
3. ★**正本がナレッジ側にある文書は Claude Code から見えない。** `docs/MEMO_INBOX.md` を見て「M-004〜M-007 は未保存」と報告された。repo に無いのは事実なので**観察は正しく、「未保存」という推論だけが誤り**
4. ★**分母を概数で焼いた。** MASTER_ADMIN「87件（うち目視50）」→実測 **90件**（.ts 46 / .tsx 44）。`session.user.role` 8件→**6件**（grep のパターン違い。減ったのではない）。`runWithoutTenantContext` 17ファイル→**42ヒット / import 20ファイル / 呼び出し19箇所**。★**src は1行も変わっていない**ことを `git diff --name-only 84e0507..d4f7610` が docs のみであること（陽性対照つき）で確認済み
5. ★**用語を誤って spec に焼いた。** 「卸値」「原価」を使い発注締日を落としたまま v1.0 を commit（a502dfb）した。慎太郎さんの訂正を受けて D-0 を新設し 3036132 で更新。**改訂履歴に「初回 a502dfb は M-007 未反映だった」を隠さず記録してある**
6. ★**作成用ガード（`if [ -f ]`）を更新用ブロックに流用し、更新が拒否された。** 拒否側に倒れたので破壊はゼロ。ガードの向きは作成／更新で逆になる
7. ★**同一出力の再提示が1回発生。** 鉄則8に従い分析を止め、旧ファイルに無い文字列（`M-007`）の件数を1行だけ直接打ってもらい、0 で上書き未達を確定させた（「冒頭5行」は新旧で共通するため判別できない）

## ⑫ 日付について（★3度焼かれた論点）

- 設計作業: **2026-09-10 14〜18時 JST**
- commit: **2026-09-11 08:48〜17:02 JST**（日またぎ）
- spec のファイル名は設計確定日（09-10）のまま、本文冒頭に両日を注記する方式を採った。**ファイル名は変えていない**
- 次セッションは冒頭で `date` を流して照合する

## ⑬ 更新したスキル（★次のチャットから有効）

- **`file-write-verification`**（944行）に、⑪の教訓1〜4と6を該当する鉄則（1 / 2 / 6 / 8）の中へ組み込む提案カードを **claude.ai 側で発行済み**
- ★**実行環境のスキルはチャット開始時点のスナップショットで固定される。** 保存しても**このチャットには反映されない。次のチャットを開いてから使う**

## ⑭ migration 運用（変更なし・毎回確認する）

- `prisma migrate dev` は**使わない**（dev に `_prisma_migrations` が無く DB 全体の reset を要求する）
- 正運用: `migrate diff` のドライラン → `db push` → **手書き** migration → `migrate diff` の出力と手書きの一致検証 → 本番は Railway の `migrate deploy`
- ★`prisma migrate reset` と `--accept-data-loss` は**実行も提案もしない**

## ⑮ 締めの状態

- ブランチは **main のまま**終えている（feature に戻していない）
- 未マージ PR **なし**
- dev / 本番 DB は**本セッションで一切触っていない**（docs のみ・src 0行・migration 0本）

## ⑯ push 後の追記（本メモ自身の commit と、その検証で出た教訓）

本文 ⓪〜⑮ は fbe8b3c の時点で書かれたため、自分自身の commit を含められなかった。その分をここに足す。

- **fbe8b3c** ― 本メモを全文上書き（173行 / 139 insertions・221 deletions。255 − 221 + 139 = 173 で算数が一致＝全文が落ちずに届いた証拠）
- **7e5e8c7** ― ⓪ の保存確認しきい値を訂正（marker は 1 ではなく **2** が正）
- 締め時点の HEAD = **7e5e8c7**。origin と同期・未 push なし・untracked なし・作業ツリー クリーン・branch=main・open PR 0件
- ⑩ の `SESSION_HANDOVER.md` 行は ★**ナレッジ差し替え済み**（2026-09-11 / claude.ai 側の project_write で実施）に更新された
- ★**教訓8: 本メモの検証期待値を自分で間違えた。** marker の期待値を「1」と書いたが実測は **2**。原因は、同じブロックで自分が ⓪ に grep のコマンド例を書いたこと。**期待値は、そのブロックで自分が追加する文言まで含めて立てる**（`file-write-verification` 鉄則2）。旧記載のままなら、次セッションが marker=2 を見て「保存されていない」と誤判定するところだった
- ★**教訓9: 本文が自分自身の commit SHA を含められない**ため、メモと実態は push 直後に必ず1段ずれる。締めの最後に本節のような追記を1本入れて閉じる
- ★⑬ の補足: 次セッションは ⓪ の直後にスキル反映を確認する（鉄則1 の「全文上書きが届かないときは追記方式に倒す」と、鉄則2 の「分母も目視数と同じ強さで測る」が本文にあるか）
- ★⑩ の補足: ナレッジ `BACKLOG.md`（2026-09-09 版・196行）の解消手順は、`git show origin/main:docs/BACKLOG.md` の全文ダンプを1回取り、claude.ai 側で差し替えること
