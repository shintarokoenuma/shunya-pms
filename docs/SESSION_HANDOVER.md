# セッション引き継ぎメモ（CLOSE-W / 2026-09-22 22時台 締め）

## ⓪ 次セッションの最初の一手

スキル `shunya-session-start` を発動する。読まずに着手しない。
★前回（本締め）の保存が実際に入ったかを最初に確かめる: `git log --oneline -4 -- docs/SESSION_HANDOVER.md docs/BACKLOG.md docs/specs/` と `tail -1 docs/SESSION_HANDOVER.md`（END-OF-HANDOVER-CLOSE-W であること）。
★本締めで既存スキル1件（shunya-design-reread）の更新カードを出した（§13）。慎太郎さんが保存していれば次のチャットから有効。

## 1. プロジェクトの棲み分け

- 本メモは **shunya-pms**（~/shunya-production-system / github.com/shintarokoenuma/shunya-pms / 本番 shunya-pms-web-production.up.railway.app）のもの。
- saagara-v2 / earnpulse / swtras-showroom とは別物。混同しない。

## 2. 本セッションでやったこと（2026-09-22 21時台〜22時台 JST・コード変更なし）

1. **Q-3（KKAP+ の消費税の端数処理）を確定した。** KKAP+ が出した実物の合計請求書で判定した（慎太郎さんの添付7枚＋共有ドライブ「請求書」フォルダ 1t8JcFXy3TkY1OLCAVLuq9AbnABH9rc6u を claude.ai 側のサブエージェントが read-only で調査・全文34件＋ヘッダ約200件。ERA 2024/09 と ACME 2023/10 は claude.ai 側で原本を再読して数字を確認）。
   - 計算の単位は **請求書ごと・税率ごとに1回**（インボイス制度開始 2023-10 以降は全件そう）。
   - 端数の向きは **取引先ごとに違う**（切り捨て: ERA・プレステージ・SHIN DESIGN WORKS／1円上げ: ベイクルーズ ACME 財務管理課。切り上げと確定できる例は無し）。
2. 実物の請求書から、仕様確認書 v1.0 に無かった形を4つ見つけ、うち3つを確定した（繰越型・返品＝赤伝・入金の単位）。
3. schema を read-only で2回実測した（21:43 請求まわりの器／22:06 納品書が数量マイナスを受け付けるか）。
4. 以上を **仕様確認書 addendum v0.1 → v0.7** にまとめた（D-22〜D-28）。
5. 慎太郎さんから「経理だけ freee 等に連動した方がいいか」の相談があり、Claude が説明した（請求までは shunya-pms・帳簿から先は作らない・受け渡しは書き出しで／分かれ目は税理士事務所の会計ソフト）。★Claude の説明であり、慎太郎さんの決定ではない。

## 3. 確定した設計（addendum v0.7・原文は同書 §1）

| D | 内容 |
|---|---|
| D-22 | 消費税は請求書1枚につき税率ごとに1回（v1.0 §2-4 のとおり） |
| D-23 | 端数処理は **クライアントごとに持つ**（既定＝切り捨て）。Client に列1本＋新しい enum |
| D-24 | 合計請求書は **KKAP+ と同じ繰越型**（前回御請求額／御入金額／繰越金額／当月お買上げ額／消費税等／今回御請求額・明細に〔御入金〕行） |
| D-25 | 入金は **クライアント単位**で記録（Payment を INCOMING・counterpart=CLIENT で単体保存・請求書に充当しない）。**入金の記録は PR-2 に含める**（v1.0 の PR-5 は PR-2 に吸収） |
| D-26 | マイナスは **赤伝**（数量マイナスの行を持つ納品書）で処理し、合計請求書に載せる。請求額のマイナスも許す |
| D-27 | OEM は基本的に返品を受けない（値引きか修理で収める）。赤伝の用途は値引きと自社ブランドの委託の戻り。委託の戻りは製品在庫になる。受注の納品済み数は自動では戻さない |
| D-28 | マイナスは **数量で表す**（単価はプラス）。値引きも「値引き　数量 −1　単価 30,000」の形（KKAP+ と同じ） |

## 4. 実測した事実（addendum v0.7 §2-3・§2-5）

- Invoice に前回御請求額・御入金額・繰越金額の列は無い。Client にも enum にも端数処理は無い。Payment は請求書に紐づけずに存在できる（counterpartType に CLIENT）。請求系4モデルは init で DDL 適用済み・以後 ALTER なし・src 実使用0件。
- 納品書の数量マイナスを止めているのは **validator の1か所だけ**（`src/lib/validators/delivery-note.ts:32` 数量 > 0、`:44` 単価 >= 0）。DB は Int・CHECK なし、action に拒む処理なし、画面に min なし。
- 納品済み数の算出し直し（`delivery-notes.ts` recomputeDeliveredQuantities 623-664）は soItemId／skuId を持つ行だけを集計 → 受注に紐づかない手入力の赤伝は受注の数に影響しない。
- 税の丸めの既存箇所: 見積書 PDF `quotation-data.ts:227`（Math.floor）／納品書 `delivery-notes.ts:581`（Math.round）。
- DeliveryNoteItem.productId は NOT NULL（値引きの行も品番を1つ選ぶ必要がある）。

## 5. 完了状態

| 内容 | 状態 |
|---|---|
| Q-3 | **確定**（D-22・D-23） |
| B-109 / B-123 | 未着手（仕様確定。PR-2 の設計が D-22〜D-28 で固まった） |
| B-114 | 進行中（残りは納品書 PDF＝PR-4） |
| コード | 本セッションの変更なし・PR なし |

## 6. 未マージ PR

- 無し（ゲート0で `gh pr list --state open` が空・22:13 JST）。

## 7. dev / 本番 DB の状態

- dev = hopper.proxy.rlwy.net:12921 / 本番 = shuttle:16099。本セッションで DB は触っていない（read-only の recon は schema ファイルと src の grep のみ）。
- dev の確認用データは前回のまま: DLV-2026-0006（葵アパレル[ダミー]・出荷済み・量産行＋手入力行）／SO-2026-0004（入力途中）。PR-2 の試しに使える。

## 8. 本日の文書

- 仕様確認書 addendum `b-109-b-114-b-123-billing-spec-addendum-v0_7-2026-09-22.md`（D-22〜D-28・実測・未決）。★ナレッジには v0.1〜v0.7 の7版がある（claude.ai 側で保存）。repo には最新の v0.7 だけを保存する（v0.7 は v0.1〜v0.6 の内容をすべて含む）。

## 9. 次にやること（優先順・冒頭に実態確認）

| 順 | 内容 | ライフサイクル | 規模 | 本番影響 |
|---|---|---|---|---|
| 0 | 実態確認（本締めの保存が入ったか・git log origin/main・gh pr list） | — | 小 | なし |
| 1 | **PR-2 の実装ブリーフ**（合計請求書＋繰越＋クライアント単位の入金＋クライアントごとの端数処理＋赤伝の validator 変更）。書く前に shunya-design-reread（migration 運用の実測・画面の文言の grep）。ブリーフで決めること: 列名・enum 名／非課税の行の紙面／納品書（四捨五入）と請求書で税額が1円ずれ得る件／マイナスの丸め（Math.round は負の .5 を 0 側に丸めない）／最初の請求書の前回御請求額（人が入れる案）／Payment の status・scheduledDate の埋め方 | 12. 請求 | 大 | 金額に直結・migration 1本（Invoice 4列＋Client 1列＋enum・すべて ADD） |
| 2 | PR-3（先の請求と前受金）の前に K-1（前受金の消費税）を税理士事務所に確認。★同時に「どの会計ソフトを使っているか」「売上・入金はどの形式が受け取りやすいか」も聞く（B-216） | 12. 請求 | 中 | 金額に直結 |
| 3 | KKAP+ の得意先マスタで確認（急がない・移行 B-215 の前）: ACME（5210300）とウエルカム（5210100）の端数が四捨五入か切り上げか／「税計算の単位」項目の有無 | 12. 請求 | 小 | なし |
| — | 仕入（B-212）の仕様確認書（請求の後にすぐ） | 8 → 12（買う側） | 大 | — |
| — | B-208（PDF フォントの欠け字）。PR-4（納品書・請求書 PDF）の前に済ませると新しい帳票にも効く | 横断（基盤） | 小〜中 | 全 PDF の見た目 |

- PR の並び（更新後）: PR-1 量産納品書（完了）→ **PR-2 合計請求書＋繰越＋入金＋端数設定＋赤伝** → PR-3 先の請求と前受金 → PR-4 納品書・請求書 PDF → PR-6 締め（PR-5 は PR-2 に吸収）。
- ★手前の空きステップ: 1（問い合わせ）・2（企画相談）・6（受注期間）・10（検品・完成品在庫）・13（分析）が未着手。★委託の戻りを製品在庫に入れる仕組み（B-221）はステップ 10 の領域で、v1 の請求には影響しない（赤伝で請求を正しくするところまで）。

## 10. ナレッジ登録状況

- addendum v0.1〜v0.7: ナレッジ `claude/` に登録済み（claude.ai 側で project_write・本セッション）。
- 本メモ: ナレッジの `SESSION_HANDOVER.md` を同じ本文で差し替える（claude.ai 側・本締め）。
- MEMO_INBOX: 本セッションの追記なし（Q の回答は addendum の D 番号に原文で記録する運用＝v0.1 の申し合わせ）。repo・ナレッジとも M-001〜M-029。
- ★BACKLOG.md: ナレッジ側は古い版のまま（前回から差し替え依頼中）。**本締めの push 後、repo の `docs/BACKLOG.md` をナレッジの `BACKLOG.md` に差し替える**（慎太郎さん）。

## 11. B-番号の増減（本セッション）

- 新規: 1件（**B-221** 委託販売と委託の戻りの製品在庫への戻し）。起票前に BACKLOG を grep（委託 0 行・製品在庫 0 行・返品 0 行・赤伝 0 行／陽性対照 完成品在庫 1 行＝B-150）。
- 状態変更: 0件
- 定義欄への追記: 4件（B-109 D-22〜D-28 と PR の並び／B-213 D-25 で前提が変わった／B-215 移行時に確かめる KKAP+ の設定／B-216 税理士事務所の会計ソフトを確認）
- 取り下げ: 0件
- 番号未採番の合意: 0件（締め前に「他に気づいた点は」を聞き「なし」）

## 12. 繰り延べた要件

- addendum v0.7 で繰り延べたもの: 委託の戻りを製品在庫に入れる → **B-221**（新規）。
- 移行時の KKAP+ 設定の確認（Q-3a / Q-3b）→ **B-215 に追記**。
- 非課税の行・1円のずれ・マイナスの丸め・最初の請求書の初期値 → 繰り延べではなく **PR-2 の実装ブリーフで決める**（B番号は振らない）。
- 計 2件（新規 1・既存への追記 1）。

## 13. 注意点・残課題・教訓

- ★**既存システム（KKAP+）の設定を実物の帳票から判定するときは、判定に使える例を選ぶ。** 添付7枚のうち6枚は税抜合計が10円単位で端数0＝向きを判定できなかった。端数 0.5〜0.9 の例を、**複数の取引先から**集めて初めて「取引先ごとに違う」が見えた。1社の例で全社の設定を決めない。
- ★KKAP+ の実物の請求書には、仕様確認書 v1.0 に無い形（繰越型・赤伝・〔御入金〕行・非課税の行）が入っていた。**v1.0 を先に書いてから実物を見た順番が逆だった。** 次に帳票を設計するとき（仕入 B-212 など）は、実物を先に見る。
- 本セッションのスキルの更新: 1件。`shunya-design-reread` に「既存システムに合わせる要件は、実物の帳票を先に見てから書く（判定に使える例を・複数の取引先から）」の節を足す更新カードを出した（claude.ai 側・propose_skills）。★保存されていなければ未反映。反映は次のチャットから（実行環境のスキルはチャット開始時点で固定される）。

## 14. ブランチ

- **main のまま終える**（docs を main に commit するため）。コード作業の再開時にその場で枝を切る。

END-OF-HANDOVER-CLOSE-W
