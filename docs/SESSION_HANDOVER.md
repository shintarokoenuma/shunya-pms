# セッション引き継ぎメモ（2026-08-19 更新）

## ⓪ 次セッションの最初の一手

shunya-session-start スキルを発動する。発動せずに指示文を出さない。

## ① プロジェクト棲み分け

- shunya-pms（~/shunya-production-system）と saagara-v2 は完全に別物
- Claude Code 向け指示書には毎回冒頭に【対象プロジェクト】ヘッダを入れる
- 指示を貼る前に VS Code のウィンドウが shunya のローカルパスか目視確認する

## ② git の状態（2026-08-19 セッション終了時点）

- ブランチ: main（★docs を main に commit した日は main のまま終える）
- main HEAD: 19d3fdf（PR #135 マージ）＋ 本日の docs commit
- open PR: 無し ★次セッション冒頭で gh pr list により再確認する
- 次に振れる番号: B-175
- 作業ツリーの untracked: skill/ 配下の zip 5本のみ
  （Claude.ai アップロード用アーカイブ。repo に入れない方針で確定）

## ③ 本日の成果

1. ★B-148 PR-1（受注の成立）を本番反映 — PR #135 マージ（19d3fdf）
   - 本番 dry-run（BEGIN/ROLLBACK・shuttle:16099）実施済み
     sales_orders_before=0 / ALTER 成功 / tx内 is_nullable=YES / ROLLBACK後 NO
   - dev 動作確認 全8項目クリア（2026-08-17〜18）
   - 本番 smoke test 完了（サイドバー「受注」表示・一覧表示・カルテ無傷）
   - ★ライフサイクル ステップ7 に初めて実装が入った
2. 受注 spec addendum v0.1 を作成（R-10・R-11 の改訂）
3. B-166〜B-174 を起票（9件）＋ B-148 を「一部完了」に状態変更

## ④ ★本日確定した設計（addendum v0.1 が正）

### 入力経路は3層（R-10 改訂）

| 層 | 経路 | B番号 |
|---|---|---|
| 本命 | 専用オーダーページ（先方入力） | B-171 |
| 移行期 | Excel 取込 | B-149 |
| 恒久 | 手入力（代打ち） | PR-1 で完成 |

★名寄せ基盤（B-170）が全経路の共通前提。先方の画面には先方の商品コードが
　必要なため、専用ページにしても名寄せは消えない。

### 同一品番の重複（R-11 改訂）

一意制約にはしない。検知して「差し替え / リピート」の2択を人に選ばせる（B-169）。
リピートは正当な業務（同一品番・別納期の追加生産）。

### 単価

品番単位で1つ入力し全 SKU に配る（実データで裏付け済み・不変）。
SoItem.unitPrice は nullable 化する（B-167）。希望数量の段階では最終コスト未確定のため。

### productionQuantity

SO 由来を正とし Sku 側の手入力を廃止する（B-168）。★PR-2 の前提。

## ⑤ 次にやること（優先順）

| 順 | 内容 | ライフサイクル | 備考 |
|---|---|---|---|
| 1 | B-168（productionQuantity の正の定義） | 7 → 8 | ★PR-2 の前提。先に決着させる |
| 2 | B-148 PR-2（発注生成の接続・B-156 と同時判断） | 7 → 8 | ★これで M4 完了 |
| 3 | B-173（動線マップ作成・docs のみ） | 全体 | 早いほど安い |
| 4 | B-174 ＋ B-135（見積の発注先・工場の選択 UI） | 5. 見積もり | ★金額直結 |
| 5 | B-143 確定見積 QE-2 | 5. 見積もり | |

★空いている手前のステップ: 1・2・5(仕様書)・6・10・12・13。
　7 は PR-1 が入り、手入力経路のみ稼働。

## ⑥ 環境

- dev DB: hopper.proxy.rlwy.net:12921（PORT 3001）
- 本番 DB: shuttle.proxy.rlwy.net:16099（★B-148 の migration 適用済み）
- ★Railway から取るのは DATABASE_PUBLIC_URL（DATABASE_URL は internal で到達不能）
- ★接続文字列は host だけでなく末尾の DB 名まで検証する（後述⑧-2）
- dev の残置データ（意図的に残す・PR-2 の検証に使う）:
  SO-2026-0001（キャンセル）／SO-2026-0002（確定受注・BLACK/S=2）
  ＋ 従来からの PE-2026-0001 由来 DRAFT PO-0008〜0011 / WO-0007〜0013

## ⑦ 気づきメモ（Google ドキュメント）

2026-08-19 時点で最終更新 2026-08-11 01:52・新規未起票項目なし（実確認済み）。
★Claude から Drive への書き戻しは不可（消し込みは慎太郎さんが行う）。

## ⑧ 本セッションの失敗（繰り返さない）

1. ★Claude が「PR はまだ open していない」と実態確認せず断定した。
   実際は指示書③の実行時（2026-08-13）に既に PR #135 が open していた。
   → PR・ブランチの状態は毎回 gh pr list で確認してから書く。
2. ★本番接続文字列の末尾1文字（railway の y）が欠けたまま dry-run を試み、
   「database "railwa" does not exist」で失敗した。
   host の確認（shuttle）だけでは検出できなかった。
   → tail -c で末尾の DB 名まで検証する。shunya-environment-safety-check に反映済み。
3. ★Claude が「見積の発注先は B-135/B-140 で受領済み」と引き継ぎメモの要約だけで断定した。
   実際は B-140 が「見積編集フォームへの入力欄」を明示的に却下しており、
   B-135 は未着手だった。→ B-174 として起票。
   → BACKLOG の定義文は要約ではなく原文を grep して読む。
4. Claude Code が「dry-run 未実施のままマージされた」と報告した。
   実際は 2026-08-18 に実施済み。Claude Code はその場にいないため観測できない。
   → Claude Code の状況認識は自セッション内に限られる。外の事実は与える。

## ⑨ ナレッジ登録の状況

★2026-08-19 に project_knowledge_search で実確認した結果

- ★新規登録が必要: sales-order-spec-addendum-v0_1-2026-08-19.md
- ★新規登録が必要: b-148-pr1-implementation-brief-2026-08-13.md（未登録だった）
- ★差し替え必要: BACKLOG.md（B-165〜B-174 反映後）
- ★差し替え必要: SESSION_HANDOVER.md（本ファイル）

## ⑩ 本セッションの増減（沈黙の禁止）

- B-番号 増減: 新規 9件（B-166〜B-174）／状態変更 1件（B-148）／取り下げ 0件／
  番号未採番の合意 0件
- 繰り延べた要件: 9件（すべて B番号を振った）
- スキルの更新: 1件（shunya-environment-safety-check）★次のチャットから有効
- ナレッジ差し替え依頼: 4件（⑨のとおり）
