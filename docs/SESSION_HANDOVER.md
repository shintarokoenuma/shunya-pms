# セッション引き継ぎメモ（2026-08-22 更新）

## ⓪ 次セッションの最初の一手

shunya-session-start スキルを発動する。発動せずに指示文を出さない。

## ① プロジェクト棲み分け

- shunya-pms（~/shunya-production-system）と saagara-v2（~/saagara-rebuild）は完全に別物
- Claude Code 向け指示書には毎回冒頭に【対象プロジェクト】ヘッダを入れる
- 指示を貼る前に VS Code のウィンドウが shunya のローカルパスか目視確認する
- ★2026-08-22 に shunya 向け指示が saagara に流れる誤送信が発生した（shunya への影響は無し・確認済み）

## ② git の状態（2026-08-22 セッション終了時点）

- ブランチ: ★main（docs を main に commit したため main のまま終える）
- ★ただし feat/b-148-pr2a-so-yield が存在し、PR #136 が open のまま残っている
- main HEAD: 本セッション締め commit
- 本日の main commit: c410555（B-175/176・B-073統合）→ 4acca93（B-168 spec v0.1）
  → 5d3bbe3（B-177〜179）→ f5b26f8（PR-2a ブリーフ・B-180）→ 締め commit
- feature ブランチ HEAD: fc9a2eb（PR-2a 実装・10 files・+626/-121）
- 次に振れる番号: B-182
- 作業ツリーの untracked: skill/ 配下の zip のみ（repo に入れない方針で確定）

## ③ 本日の成果

1. B-168（productionQuantity の正の定義）を仕様確定し spec v0.1 を保存（4acca93）
   - D-1〜D-9 を確定。SO 由来を正とし Sku.productionQuantity はキャッシュ列
2. B-148 PR-2a 実装ブリーフを作成（f5b26f8）し、実装して PR #136 を open（fc9a2eb）
   - ★未マージ。dev 自動検証は完了、目視 §11-4〜10 が未了
3. B-175〜B-181 を起票（7件）。B-073 に跨ぎ材料発注の論点を統合
4. ★B-180 を取り下げ（B-164 と重複していた）
5. 気づきメモ（Google ドキュメント）の未起票3項目を回収し起票

## ④ ★本日確定した設計

### B-168 v0.1（4acca93・docs/specs/b-168-production-quantity-spec-confirmation-v0_1-2026-08-19.md）

| # | 確定内容 |
|---|---|
| D-1 | SO 由来を正とする。Sku.productionQuantity は Σ SoItem を書き戻すキャッシュ列（列は残す） |
| D-2 | 数量マトリクス下段の手入力 UI を非表示にする |
| D-3 | 歩留まりは SoItem（SKU 行）単位で保持 |
| D-4 | 率(%)と加算枚数(+N)の2方式。★既定は addendum で改訂（下記） |
| D-5 | yieldMode / yieldQuantity を追加。B-167 と同一 migration |
| D-6 | RATE は ceil(ordered × (1+rate/100))・SKU 単位切り上げ。QUANTITY は ordered + N |
| D-7 | 入力 UI は一律／色別／サイズ別／SKU 個別の一括適用 |
| D-8 | 発注生成での上書きは R-d のとおり維持（新規実装なし） |
| D-9 | 減産は SoItem.productionQuantity を動かす。BomItem.lossRate には触らない |

### ★B-168 addendum v0.1（本日締めで保存）

- **D-4 の改訂**: 既定を「率 5%」から「**加算枚数 0 枚**」に変更
  - 根拠: 慎太郎さんの実務情報「オーダーの8割程度は指定数」
  - 原設計 §2.6 の標準5% は展示会積み上げ前提の記述であり、受注 spec v1.0 §0 の
    訂正と同じ構図で実態と合わない
- **★この改訂はまだ実装されていない**（下記 ⑤-1）

### 受け入れた副作用（意図的）

品番カルテの「資材所要量」「1枚原価」は、SO 成立前の品番で 0 表示になる
（products/[id]/page.tsx:578 が無条件描画するため）。量産見積側は syntheticSku で
自前の分母を持つため影響なし。解消は B-177。

## ⑤ 次にやること（優先順）

| 順 | 内容 | ライフサイクル | 備考 |
|---|---|---|---|
| 1 | ★PR #136 に既定値の変更を積む（D-4 改訂の実装） | 7 → 8 | ★マージ前の必須作業 |
| 2 | PR #136 の dev 目視（§11-4〜10） | 7 → 8 | 慎太郎さんのブラウザ操作 |
| 3 | 本番 dry-run（CREATE TYPE 含む5文） | 7 → 8 | triple-gate step 2 |
| 4 | PR #136 マージ → 本番確認 | 7 → 8 | ★不可逆 |
| 5 | B-148 PR-2b（SO→発注の接続・B-156 同時判断） | 7 → 8 | ★これで M4 完了 |
| 6 | B-173（動線マップ・docs のみ） | 全体 | 早いほど安い |
| 7 | B-181（資材のサイズ軸） | 8 / BOM | 案A で先送り確定 |

★空いている手前のステップ: 1・2・5(仕様書)・6・10・12・13。
　7 は PR-1 が本番稼働、PR-2a が PR 段階。

### ⑤-1 ★PR #136 マージ前の必須作業（未実装）

sales-order-form.tsx の初期値を「一律・率・5」から「一律・加算枚数・0」に変更する。
validator 側に既定値があれば同時に変更する。tsc / build を通してから同ブランチに積む。
★これを入れずにマージすると、既定 5% の状態で本番に出る。

## ⑥ 環境

- dev DB: hopper.proxy.rlwy.net:12921（PORT 3001）★PR-2a の db push 適用済み
- 本番 DB: shuttle.proxy.rlwy.net:16099（★PR-2a の migration は未適用）
- ★Railway から取るのは DATABASE_PUBLIC_URL。末尾の DB 名まで tail -c で検証
- ★取得は pbpaste を使う。cat > の対話入力方式は指示に書かない
- dev の残置データ: SO-2026-0001(CANCELLED) / 0002(CONFIRMED) / 0003(TENTATIVE)
  ＋ 本日の目視で 0004 以降が増えている可能性あり
  ★0003 は前回メモに記載が無く、その後に増えたもの

## ⑦ 気づきメモ（Google ドキュメント）

2026-08-22 に読み込み。未起票だった3項目を回収し起票済み
（B-073 統合 / B-175 / B-176）。★前回メモの「新規未起票項目なし」は誤りだった。
★Claude から Drive への書き戻しは不可（消し込みは慎太郎さんが行う）。

## ⑧ 本セッションの失敗（繰り返さない）

1. ★B-180 を B-164 と重複起票した。spec §9 の「未起票（軽微）」という記述だけを
   信じ、BACKLOG 原文を grep しなかった。→ 起票前に必ず同義の既存行を grep する。
   spec の「未起票」表記は書かれた時点の情報でしかない。
2. ★spec のファイル名を 2026-08-19 で作った。実際の作成日は 2026-08-22。
   前回の引き継ぎメモの日付を今日だと思い込んだ。→ 日付は必ず現在日時で確認する。
   （b-168 spec v0.1 と pr2a ブリーフの2件が該当。リネームはせず記録のみ）
3. ★同一の Claude Code 出力が4回にわたり繰り返し届いた。私はそのたびに再送を
   依頼したが、原因（指示が届いていない）の切り分けが遅れた。
   → 直前と同一の出力は検証材料として扱わない。file-write-verification に追記済み。
4. ★c410555 で実行済みの起票を「未実行の可能性が高い」と断定し再送した。
   raw 出力が返らないことを未実行の根拠にした。→ 実態は grep で確認してから言う。
5. ★shunya 向け指示が saagara に誤送信された。shunya への影響は確認済みで無し。

## ⑨ ナレッジ登録の状況

★2026-08-22 に project_knowledge_search で実確認した結果

- ★新規登録が必要: b-168-production-quantity-spec-confirmation-v0_1-2026-08-19.md
- ★新規登録が必要: b-148-pr2a-implementation-brief-2026-08-19.md
- ★新規登録が必要: b-168-production-quantity-spec-addendum-v0_1-2026-08-22.md
- ★差し替え必要: BACKLOG.md（B-175〜B-181 反映後）
- ★差し替え必要: SESSION_HANDOVER.md（本ファイル）

## ⑩ 本セッションの増減（沈黙の禁止）

- B-番号 増減: 新規 7件（B-175〜B-181）／状態変更 1件（B-073）／
  取り下げ 1件（B-180・B-164 と重複）／番号未採番の合意 0件
- 繰り延べた要件: 4件（B-177・B-178・B-179・B-181／全件 B番号を振済み）
- スキルの更新: 2件（file-write-verification / shunya-session-close）
  ★次のチャットから有効
- ナレッジ差し替え依頼: 5件（⑨のとおり）

## ⑪ 未確定・持ち越し

- B-167 の突合警告（QE-2 との照合）は B-143 待ち。PR #136 がマージされた時点で
  B-167 は「一部完了」（nullable 化のみ完了・突合警告は残る）とする
- B-164 は PR #136 のコードで既に修正済み。マージ時に「完了」にする
- B-181（資材のサイズ軸）は案A（起票のみ・先送り）で確定。
  b-067 v1.0 D1 の改訂 addendum が別途必要
