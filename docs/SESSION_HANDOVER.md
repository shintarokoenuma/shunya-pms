# セッション引き継ぎメモ（CLOSE-Z・2026-09-24 20時台 JST）

## ⓪ 次セッションの最初の一手

★**2026-09-25 00時台に CLOSE-Z2 を末尾 §17 に追記した。§9 の「次にやること」は §17-6 が上書きする。§17 から読む。**

★**何よりも先に `shunya-session-start` を発動する。** 読まずに答え始めない。日付は必ず `date` で取り直す。
★**その次に §15 を読む。** 今日スキルを4本更新した（次のチャットから有効）。

## 1. プロジェクトの棲み分け

- 本件は **shunya-pms**（~/shunya-production-system・github.com/shintarokoenuma/shunya-pms）
- saagara-v2 / earnpulse / swtras-showroom は**別プロジェクト**。スキルもワークフローも別

## 2. 本セッションでやったこと（2026-09-24 13時台〜20時台 JST）

★前回 CLOSE-Y の後、別セッションで **B-226（製品名の中立化・#164 = 041af38）が完了**し、メモに §16 が足されている（a0b073e）。その §16 は本メモでは要約しか持たない。原文は `git show a0b073e:docs/SESSION_HANDOVER.md` で読む。

1. **メモ受信箱**: M-030（コメントのテンプレート）・M-031（単位のプルダウン）・M-032（インスタ用デモ）・M-033（携帯の見え方）を記録。M-030〜M-032 は B-227〜B-229 に起票、M-033 は既存 B-093 に紐づけ
2. **インスタ宣伝用のデモデータ（B-229）**: dev 専用シード `scripts/seed-dev-demo-data.ts` を作成（PR #165・**未マージ**）。架空3ブランド（ÉTÉ BLANC / HINOKI WORK SUPPLY / SLOW CURRENT）・2工場（Saigon Linen Works / 瀬戸内ソーイングラボ）・6品番・量産見積3・受注1を dev に投入済み
3. 絵型の線画2枚（主役のリネン開襟シャツ・カバーオール）を claude.ai 側で作成。慎太郎さんが4品番にアップロード済み
4. 慎太郎さんが量産発注生成を3品番で実行（PO / WO / 進行が揃った）

| commit | 内容 |
|---|---|
| 24da95e（main） | MEMO_INBOX に M-030 / M-031 |
| f31dc75（main） | デモ用データ投入 実装ブリーフ v1.0 |
| 8d790b4（main） | MEMO_INBOX に M-033 |
| 0830dc9 / 131a490 / fd8365e（feat/demo-seed） | シード本体・架空性の修正・量産見積と受注の追加 |

## 3. 確定した設計（D-1〜D-18・原文はデモ用ブリーフ）

- ★既存の dev 検証データ（［ダミー］一式）には触らない。デモは別コードで横に足す（D-1）
- ★スクリプトで入れる: マスター・品番・色・SKU・BOM・縫製指示・メモ・量産見積・受注。画面で入れる: 絵型・量産発注生成・納品書・請求書（D-4 / D-8 / D-15〜D-17）
- ★量産見積は**画面からは確定サンプル経由でしか作れない**（`createProductionEstimateFromSample`）。デモはシードで作り、金額は画面と同じ `computeProductionEstimate` の結果を保存（D-15）
- ★受注は CONFIRMED で作り、`recomputeSkuOrderedQuantities` と同じ集計で SKU の受注数を更新（D-13 / D-16）
- ★架空の登録番号は実在法人と一致しうるので入れない。電話は 03-0000-xxxx 形式、住所は丁目まで

## 4. 実測した事実

- dev はローカルから 1 往復約 1.8 秒（リモート proxy）。スクリプトの受注トランザクションが画面と同じ 15 秒で P2028 になった → 180 秒に延長（D-18）
- 一覧の並びはすべて新しい順。量産見積・見積・受注の一覧には**絞り込みが無い**（PO は発注先、WO は状態、請求はクライアントで絞れる）
- 量産発注生成には**再生成ガードが無い**。シャツとカバーオールで2回押され PO / WO が二重になった（既存 **B-142** の実例）
- PO は `product_id` 列を持たず `primary_product_id` で品番に紐づく

## 5. 完了状態

- B-229（デモ）: **進行中**。dev 投入・絵型4品番・量産発注生成まで済み。残りは §9
- それ以外のコード変更なし

## 6. 未マージ PR

- **#165**（feat/demo-seed・3 commits・OPEN）: シード＋ブリーフ §7・§8。マージで本番に反映されるのはスクリプトと docs だけで、本番では実行しない
- マージ前の確認: `git switch feat/demo-seed` → `PORT=3001 npm run dev` → http://localhost:3001/products/2cbb64eb-b597-41d0-a94a-471f61bedbfb （主役）

## 7. dev / 本番 DB の状態（★この節が host ↔ 環境の唯一の正）

| 環境 | host | 状態 |
|---|---|---|
| dev | `hopper.proxy.rlwy.net:12921` | ［ダミー］: clients 2・納品書 live 9（1件 soft delete）・invoices 2・payments 3（すべて消してよい。ただし B-225 が入るまで入金は画面から消せない）。**デモ**: CL-003〜005・FC-003/004・SP-003/004・MT-004〜014・品番6（ETB/HNK/SLC-27SS-…-001）・PE-2026-0004〜0006・SO-2026-0006（確定）・PO-2026-0016〜0025・WO-2026-0020〜0024（すべて DRAFT・シャツとカバーオールは二重） |
| 本番 | `shuttle.proxy.rlwy.net:16099` | 今日は触っていない。前回時点で delivery_notes 0 / invoices 0 / payments 0・clients 8社すべて ROUND_HALF_UP |

★dev サーバは **PORT=3001**。接続文字列は必ず `DATABASE_PUBLIC_URL`。`tail -c` の桁数を増やさない。

## 8. 本日の文書

- `docs/specs/demo-data-seed-implementation-brief-2026-09-24.md`（v1.0 は main、**§7・§8 は feat/demo-seed**＝PR #165 のマージで main に入る）
- `docs/MEMO_INBOX.md`（M-030〜M-033）

## 9. 次にやること（優先順・冒頭に実態確認）

| 順 | 内容 | ステップ | 規模 | 本番影響 |
|---|---|---|---|---|
| 0 | `git log origin/main --oneline -5` / `gh pr list --state open` で実態確認 | — | 小 | 無し |
| 1 | **PR #165** を localhost:3001 で確認してマージ | 横断（B-229） | 小 | スクリプトと docs のみ |
| 2 | デモの残り: 二重 PO / WO の扱いを決める → 納品書 → 請求書を画面から入力 → 撮影（品番カルテ・見積 PDF・縫製仕様書・請求書画面・入力画面）→ SLC の絵型 | 11. 納品 / 12. 請求（B-229） | 中 | 無し（dev） |
| 3 | **B-215**: 本番で ERA とプレステージの端数処理を「切り捨て」に直す（前回からの持ち越し） | 12. 請求 | 小 | ★本番の書き込み |
| 4 | **B-225**: 入金の訂正・取消 | 12. 請求 | 中 | マージで本番反映 |
| 5 | B-109 PR-3（先の請求と前受金）／PR-4（納品書・請求書 PDF）／PR-6（締め） | 12. 請求 | 大 | 同上 |
| 6 | **B-093** モバイル対応の仕様確認書（1本目はサイドバーのみを提案） | 横断 | 中 | 同上 |
| 7 | B-227（コメントのテンプレート）／B-228（単位のプルダウン） | 5. 見積 起点 | 小〜中 | 同上 |

★手前の空きステップ: **10. 検品（B-150）** が未着手のまま。

## 10. ナレッジ登録状況

- **新規登録**: `demo-data-seed-implementation-brief-2026-09-24.md`（§7・§8 入りの 157 行版。claude.ai 側で作成して提示）
- **差し替え**: `claude/MEMO_INBOX.md`（M-033 と B番号を反映した全文・claude.ai 側で作成して提示）／本メモ `SESSION_HANDOVER.md`（CLOSE-Y → CLOSE-Z）／`BACKLOG.md`（B-227〜B-229）
- **前回からの持ち越し（未登録のまま）**: `b-109-b-114-b-123-billing-spec-addendum-v1_0-2026-09-23.md`・`b-222-pr2d-implementation-brief-2026-09-23.md`・`b-224-b-223-implementation-brief-2026-09-23.md`（v1.2）。★登録済みかは次セッションで project_knowledge_search で確かめる

## 11. B-番号の増減（本セッション）

- 新規 **3件**: B-227（コメントのテンプレート）・B-228（単位のプルダウン）・B-229（インスタ用デモデータ・進行中）
- 状態変更 **0件**（B-226 の完了は別セッション a0b073e で反映済み）
- 取り下げ **0件**
- 番号未採番の合意 **0件**（M-033 は既存 B-093 に紐づけ）

## 12. 繰り延べた要件

- デモ用ブリーフ §5 スコープ外: 本番への投入（要件ではない）・請求書 PDF（**B-109 PR-4 が受ける**）・［ダミー］の改名（要件ではない）
- 量産見積・見積・受注一覧の絞り込み不足は、撮影は並び順で回避できるため起票していない（慎太郎さんの要望が出たら起票する）
- ★**未起票ゼロ。**

## 13. 注意点・残課題・教訓

1. ★**デモは dev だけ。** 本番の画面は実在のクライアント・売値が写るので撮影に使わない
2. ★**量産発注生成は押すたびに PO / WO を新しく作る（B-142）。** 二重になったシャツとカバーオールの DRAFT をどうするかは慎太郎さん判断
3. ★シャツの提示単価 4,800 は自動単価 5,531.64 を下回る（原価に対し約13%）。見栄えが気になるなら見積を直す
4. ★dev に投げる SQL のテーブル名・列名は推量しない（今日 `boms` / `product_status_histories` / `client_name` / `purchase_orders.product_id` の4本を外した）。`schema.prisma` の `@@map` / `@map` から取る
5. ★ローカルのスクリプトでトランザクション内に行ごとのクエリを並べると、dev では待ち時間で落ちる（§4）
6. Claude Code の commit trailer は `Claude Fable 5.1`。履歴の書き換えはしない

## 14. ブランチ

**main のまま終えている。** feat/demo-seed は PR #165 のため残す（削除しない）。untracked 0件（ゲート0で確認）。

## 15. スキルの反映（★次のチャットから有効）

claude.ai 側で zip を作成して提示した（慎太郎さんがスキル編集画面でアップロードする）。

- `shunya-environment-safety-check` v2: ルール 00-3（dev はリモート proxy 越し・スクリプトの tx の待ち時間・SQL は @@map から）
- `shunya-design-reread`: ブリーフ内の D番号どうしの矛盾を突き合わせる／read-only スクリプトの引数を呼び出し元と同じ計算で作る（前回 §15 の持ち越し）
- `file-write-verification`: 停止条件の語が他箇所にも出る場合は行頭アンカーで判定（前回 §15 の持ち越し）
- `shunya-session-start`: Mac と claude.ai の時計が1時間ずれることがある（前回 §15 の持ち越し）
- ★**更新したスキルは、このチャットには反映されない。次のチャットを開いてから有効になる。**
- ★前回 §15 の注意は継続: `anthropic-skills` は claude.ai 同期のスキル群。ローカルの synced ファイルを書き換えても次の同期で戻る。`~/.claude/skills/` 直下の古い個人コピー6本は未処理

## 17. CLOSE-Z2 追記（2026-09-24 21時台〜09-25 00時台 JST）

★この節は CLOSE-Z の後に同じ日の別チャットで行った作業。§9 の「次にやること」は 17-6 が上書きする。

### 17-1. やったこと
- 製品展開の構想（M-034・M-035）: システムを利用者別に3エディション（Studio＝ブランド・デザイナー／Hub＝OEM・生産管理＝shunya／Floor＝縫製工場）で作る構想と、ブランド向けは saagara の受注と連動させて受注・生産・納品を一括で行う構想。名前候補は SEAM / MUSUBI / WARP / TOILE、ロゴ案は claude.ai の Design キャンバス「shunya-pms ロゴ」（4案×エディション別マーク）。★決定はまだ。実装は shunya 社内で13工程が回ってから
- テナント分離の read-only 棚卸し（M-036）→ 方針を「companyId の手書きを正式ルールにする」に決定（慎太郎さん承認）→ B-230〜B-232 起票
- B-230（PR-1）実装・マージ: PR #166 = 1679c43

### 17-2. 実測した事実（M-036 の要点）
- src/lib/prisma.ts の Extension（companyId / deletedAt の自動注入・物理削除の例外化）は withTenantContext で包んだ時だけ動く。包んでいるのは brands.ts / clients.ts の2ファイルだけ。他の76ファイルは companyId の手書きで、ほぼ全箇所が正しい
- EXTERNAL ロールの拒否はコメント・会社設定の2ファイルだけ。原価系 action にも到達できる
- 物理削除 22箇所。clients.ts:520 / brands.ts:442 は bypass を明示した意図どおりの本削除（所有確認あり）。残り20箇所は未確認（B-232）

### 17-3. 完了状態
- B-230 完了: 受注作成の取引先確認と明細 SKU の確認（作成・更新の両方）に companyId を明示、誤解コメント3箇所を修正、AGENTS.md に「テナント分離（companyId）の書き方」節を追加
- dev 確認済み: SO-2026-0007（ÉTÉ BLANC × リネンワイドパンツ）を作成 → 編集（晒し S 10→12・総数22）→ キャンセルまで通った
- ★本番の確認（受注一覧が開くか）は未報告

### 17-4. 未マージ PR
- #165（feat/demo-seed）のみ。§6 のとおり

### 17-5. dev / 本番 DB
- dev: §7 に加えて SO-2026-0007（キャンセル済み・確認用）が1件増えた
- 本番: 今回は書き込みなし（#166 のコードが乗っただけ・schema 変更なし）

### 17-6. 次にやること（§9 を上書き）
| 順 | 内容 | ステップ | 規模 | 本番影響 |
|---|---|---|---|---|
| 0 | git log origin/main / gh pr list で実態確認 | — | 小 | 無し |
| 1 | 本番の受注一覧が開くか（B-230 の smoke・閲覧のみ） | 7. 受注確定 | 小 | 閲覧のみ |
| 2 | §9 の順1〜7（PR #165 → デモの残り → B-215 → B-225 → B-109 → B-093 → B-227/228） | §9 のとおり | — | §9 のとおり |
| 3 | B-231: 書き忘れ検出チェック＋brands/clients の Extension の扱い。★B-171 / B-172 の本番投入より前 | 横断 | 中 | マージで本番反映 |
| 4 | B-232: 物理削除20箇所の意図確認 | 横断 | 中 | 同上 |
| 5 | 3エディションの名前・ロゴの決定（M-034）／saagara 連動の整理（M-035） | 横断（構想） | — | 無し |
★手前の空きステップは §9 と同じ（10. 検品 B-150）。

### 17-7. 注意点・教訓
1. ★**B-172 が実装されるまで、EXTERNAL の User を本番に作らない。** 作った瞬間に原価・仕入先・工場が見える（B-172 行にも追記済み）
2. ★新しく書くクエリは companyId と deletedAt: null を手書きする。「TENANT なので自動注入される」前提を置かない（AGENTS.md）
3. ★M番号・B番号はナレッジではなく repo の現物の最大+1 で振る。今回ナレッジの MEMO_INBOX が古く、M-032/033 を二重に振りかけた（ガードで停止・破壊なし）
4. キャンセル済み受注にも「キャンセル」「編集」ボタンが出る（M-037・未起票）

### 17-8. ナレッジ登録状況
- 新規登録: b-230-implementation-brief-2026-09-24.md
- 差し替え: SESSION_HANDOVER.md（CLOSE-Z → CLOSE-Z2）／MEMO_INBOX.md（M-034〜M-037）／BACKLOG.md（B-230〜B-232・B-172 追記）／AGENTS.md（テナント分離の節。ナレッジ側は 20260516 版のまま）
- §10 の持ち越し3件は引き続き要確認

### 17-9. スキルの反映（★次のチャットから有効）
- memo-inbox r2: 「番号は repo の現物で振る」節を追加（zip を claude.ai 側で提示）

### 17-10. ブランチ
main のまま終えている。feat/b-230-tenant-scope-sales-orders は削除済み。

END-OF-HANDOVER-CLOSE-Z2
