# セッション引き継ぎメモ（CLOSE-V / 2026-09-22 20時台 締め）

## ⓪ 次セッションの最初の一手

スキル `shunya-session-start` を発動する。読まずに着手しない。
★前回（本締め）の保存が実際に入ったかを最初に確かめる: `git log --oneline -3 -- docs/SESSION_HANDOVER.md` と `tail -1 docs/SESSION_HANDOVER.md`（END-OF-HANDOVER-CLOSE-V であること）。
★本締めで既存スキル2件の更新カードを出した（§13）。慎太郎さんが保存していれば次のチャットから有効。

## 1. プロジェクトの棲み分け

- 本メモは **shunya-pms**（~/shunya-production-system / github.com/shintarokoenuma/shunya-pms / 本番 shunya-pms-web-production.up.railway.app）のもの。
- saagara-v2 / earnpulse / swtras-showroom とは別物。混同しない。

## 2. 本セッションでやったこと（2026-09-22 14時台〜20時台 JST）

1. B-054 を完了にした（PR-4c の本番確認・22ab58f）。WO 一覧で中身が分からない件（M-026）を B-196 に追記（d0470aa）。
2. 納品書・請求書・合計請求書（ライフサイクル 11・12）の仕様を、1問ずつの質問で確定した。**仕様確認書 v0.1 → v1.0**（D-1〜D-21）。参考画面（明細の3案・案A を採用）: https://claude.ai/artifact/1J82XNzA6tRriqkBRfD1U8
3. 経理の実態を確認: 請求書は今 **KKAP+** で出し、帳簿は**税理士事務所**。freee 等は使っていない。当面は KKAP+ と**併用**し、KKAP+ の DB へは書かない（D-18）。
4. 仕入（発注に対する仕入の計上・照合・支払）は**別の仕様確認書**にする（M-029 → B-212）。
5. **B-114 PR-1（量産の納品書）を実装・マージ**: PR #156（squash 771212c）。dev で8項目を確認（保存列を read-only SQL で 0→2→0 と確認・監査ログで状態変更4回を確認）・本番で表示を確認。
6. 一覧・新規画面の説明文が「サンプル専用」のまま残っていたのを慎太郎さんが本番で指摘 → **PR #157（squash 5ce762a）** で修正・本番で確認。

## 3. 確定した設計（詳細は仕様確認書 v1.0）

- 請求額＝**改訂後の売値×枚数**（基本③・稀に④）。売値を直すのは**納品の時**＝納品書の行ごとの単価（D-1・D-2）。
- 請求は**月次の合計請求書**が基本。代引き・先払い・デポジットは納品前に請求（金額は人が入れる）（D-3・D-7・D-8）。先に請求した分は最終の請求で差し引く（D-20）。
- サンプルと量産を1枚に合算（D-4）。候補は納品完了以降の納品書（D-6）。自社の登録番号 T2011001051698（D-5）。
- 消費税の端数処理は KKAP+ に合わせる（D-10・★Q-3 未回答）。前受金の消費税は税理士事務所に合わせる（★K-1 未回答）。
- 入金の記録は v1 に入れる（D-11）。納品書 PDF と請求書 PDF は一緒に作る（D-12）。
- 締めは月ごとのロックまで・取引先ごと・締めは誰でも／解除は OWNER・ADMIN・PO/WO は発注日で月を切る・締めた期間は新規作成も止める（D-13〜D-16・D-21）。
- 量産の納品書: 1 SKU＝1行（D-19）。納品完了にした時に受注明細と SKU の納品済み数を算出し直す（D-17）。
- 請求系モデル（Invoice / InvoiceItem / Payment / InvoicePayment）は DDL 適用済み・src 実使用ゼロの休眠。締めの器だけ無い（PR-6 で ADD TABLE）。
- 実装順: PR-1 量産納品書（**完了**）→ PR-2 合計請求書 → PR-3 先の請求と前受金 → PR-4 納品書・請求書 PDF → PR-5 入金 → PR-6 締め。

## 4. 完了状態

| 内容 | 状態 |
|---|---|
| B-054（縫製仕様書 PDF） | **完了**（22ab58f） |
| PR #156（B-114 PR-1・量産の納品書） | **マージ済み 771212c**・dev 8項目・本番で表示確認 |
| PR #157（納品書の説明文） | **マージ済み 5ce762a**・本番で確認 |
| B-114 | 進行中（残りは納品書 PDF＝仕様確認書 PR-4） |
| B-109 / B-123 | 未着手（仕様確定。PR-2〜PR-6 で実装） |

## 5. 未マージ PR

- 無し（本締めのゲート0で `gh pr list --state open` が空・20:34 JST）。

## 6. dev / 本番 DB の状態

- dev = hopper.proxy.rlwy.net:12921 / 本番 = shuttle:16099。本セッションの migration: **無し**（schema 変更ゼロ）。
- ★dev に確認用データを残してある: **DLV-2026-0006**（葵アパレル[ダミー]・量産行 SO-2026-0002 の SKU 2枚 ¥1,200＋手入力行 4枚 ¥100・状態は出荷済み）。SO-2026-0002 の受注明細は納品済み 0・残り 2（出荷済みなので引当中 2）。PR-2（合計請求書）の試しに使える（納品完了にすれば請求の候補になる）。
- ★dev の SO-2026-0004（入力途中・数量100・単価なし）は画面で確定にすれば2件目の素材になる。
- 本番: 本セッションで DB は触っていない（画面の確認のみ・保存なし）。

## 7. 本日の文書（repo docs/specs/ ・ナレッジ claude/ と同じ本文）

- 仕様確認書 `b-109-b-114-b-123-billing-spec-confirmation-v0_1-2026-09-22.md`（たたき台・Q の原文）
- 仕様確認書 `b-109-b-114-b-123-billing-spec-confirmation-v1_0-2026-09-22.md`（確定・D-1〜D-21・PR 分割）
- 実装ブリーフ `b-114-pr1-implementation-brief-2026-09-22.md`（PR #156 に同梱済み）

## 8. 次にやること（優先順・冒頭に実態確認）

| 順 | 内容 | ライフサイクル | 規模 | 本番影響 |
|---|---|---|---|---|
| 0 | 実態確認（git log origin/main / gh pr list / 本締めの保存が入ったか） | — | 小 | なし |
| 1 | ★Q-3 を慎太郎さんに聞く（KKAP+ の消費税の端数処理＝請求書ごとか明細ごとか・切り捨て／四捨五入／切り上げ）。答えを仕様確認書 v1.0 の addendum に書く | 12. 請求 | 小 | なし |
| 2 | 合計請求書（仕様確認書 PR-2・B-109）の実装ブリーフ → 実装。★replacesInvoiceId（仮名）の ADD COLUMN を含む＝migration 1本（shunya-environment-safety-check ルール 00） | 12. 請求 | 大 | 金額に直結・schema 追加 |
| 3 | PR-3（先の請求と前受金）の前に K-1（前受金の消費税）を税理士事務所に確認してもらう | 12. 請求 | 中 | 金額に直結 |
| — | 仕入（B-212）の仕様確認書（請求の後にすぐ） | 8 → 12（買う側） | 大 | — |
| — | B-208（PDF フォントの欠け字）。★PR-4（納品書・請求書 PDF）の前に済ませると新しい帳票にも効く | 横断（基盤） | 小〜中 | 全 PDF の見た目 |

- ★手前の空きステップ: 1（問い合わせ）・2（企画相談）・6（受注期間）・10（検品）・13（分析）が未着手。

## 9. ナレッジ登録状況（claude.ai 側で実施・本締め）

- 仕様確認書 v0.1 / v1.0・実装ブリーフ PR-1: ナレッジ側（claude/…）に登録済み。repo へは本締めで同じファイル（sha256 を保存スクリプトが照合）を保存。
- MEMO_INBOX.md: M-026〜M-029 を追記済み・本締めで M-012 / M-027 / M-028 / M-029 の状態欄を起票結果に更新（ナレッジ側が正本）。**本締めで同じ本文を repo `docs/MEMO_INBOX.md` に書き込む**（repo は M-016 までだった）。
- 本メモ（SESSION_HANDOVER.md）: ナレッジ側を同じ本文で差し替える（claude.ai 側）。
- ★BACKLOG.md: ナレッジ側は CLOSE-U の版のまま（B-054 完了・B-196 追記・本締めの差分が未反映）。**本締めの push 後、慎太郎さんが repo の `docs/BACKLOG.md` をナレッジの `BACKLOG.md` に差し替える**（依頼済み）。

## 10. B-番号の増減（本セッション）

- 新規: 9件（B-212 仕入／B-213 入金の複数請求書への消し込み画面／B-214 締め後変更のレポート／B-215 KKAP+ からの移行／B-216 税理士への書き出し／B-217 L/C・カスタム条件／B-218 取引先ごとの請求計算の型／B-219 請求書の承認とメール送付／B-220 受注数を超える引き当ての扱い）
- 状態変更: 2件（B-054 進行中 → 完了・22ab58f／B-114 未着手 → 進行中・本締め）
- 定義欄への追記: 7件（B-196・d0470aa／B-108 / B-109 / B-123 / B-186 / B-201 / B-114・本締め）
- 取り下げ: 0件
- 番号未採番の合意: 0件（★納品書一覧で量産を見分ける印・絞り込みは Claude が提案しただけで慎太郎さんの返事が無い＝合意ではないため起票していない）

## 11. 繰り延べた要件

- 仕様確認書 v1.0 §5 の10件と実装ブリーフ §6 の2件、計12件（新規の B番号 9件・既存の番号で受けるもの 3件）:
  - 仕入 → **B-212**／消し込み画面 → **B-213**／締め後レポート → **B-214**／KKAP+ 移行 → **B-215**／税理士への書き出し → **B-216**／L/C・カスタム → **B-217**／取引先ごとの計算の型 → **B-218**／承認・メール送付 → **B-219**
  - 検品 → 既存 **B-150**／多通貨・輸出インボイス → 既存 **B-110**（追記なし・仕様確認書に番号を明記済み）
  - 受注の状態を納品で進めるか → **B-186 に追記**／受注数を超える引き当て → **B-220**

## 12. 注意点・残課題・教訓

- ★**機能を足した画面の説明文が古いまま残った**（納品書一覧「サンプル・見本類の納品書」・新規画面「引き当ては後続」）。実装ブリーフに画面の文言を入れていなかった。PR #157 で直した。→ shunya-design-reread に「画面の説明文・ボタン名を grep してブリーフの変更対象に入れる」を追記するカードを出した。
- ★**画面の数（候補ダイアログの納品済・引当中）はその場の算出で、DB の保存列とは別物**。保存列を書く機能の確認は read-only SQL で「操作前 → 操作後 → 戻した後」を取る。監査ログ（audit_logs の STATUS_CHANGE）を並べると、何回状態を変えたか・どの時点の値かが確定する（今回 20:04 の SQL は戻す操作の前で、20:09 に再取得して確定した）。
- ★Claude の説明の失敗（訂正済み）: 確認手順 5〜8 を表で一度に出して伝わらなかった／返答を英語で出した。→ 手順は順番を固定して短く、1回に1〜2手。shunya-pr-url-checklist に追記するカードを出した。
- `"use server"` のファイルは async 関数しか export できない（定数は validator へ）。拡張した prisma の tx の型は `Parameters<Parameters<typeof prisma.$transaction>[0]>[0]`（`Prisma.TransactionClient` は通らない）。
- 仕様確認書の Q を1問ずつ AskUserQuestion で聞き、回答をその場で MEMO_INBOX と仕様確認書に原文で書く運用はうまく回った。

## 13. 本セッションで更新したスキル

- 既存スキル2件の更新カードを出した: `shunya-design-reread`（画面の文言をブリーフの変更対象に入れる）／`shunya-pr-url-checklist`（保存列は SQL の3点で見る・手順は短く順番を固定）。慎太郎さんが保存していなければ未反映。
- スキルを更新した場合、**反映は次のチャットから**（実行環境のスキルはチャット開始時点で固定される）。

## 14. ブランチ

- **main のまま終える**（docs を main に commit するため）。コード作業の再開時にその場で枝を切る。

END-OF-HANDOVER-CLOSE-V
