# shunya-pms 引き継ぎメモ（2026-09-12〜13 設計作業分 / 締め 2026-09-13 JST）

★本メモにしかない識別文字列: HANDOVER-2026-09-13-CLOSE-M

## ⓪ 次セッションの最初の一手（これより先に何もしない）

1. スキル `shunya-session-start` を発動する
2. 前回の締めが保存されたかを確認する（`file-write-verification` 鉄則12）

       cd ~/shunya-production-system
       git log --oneline -3 -- docs/SESSION_HANDOVER.md
       grep -c '^## ' docs/SESSION_HANDOVER.md
       grep -c 'HANDOVER-2026-09-13-CLOSE-M' docs/SESSION_HANDOVER.md

   3つ目が 0 なら本メモは保存されていない（★正常時は 2。冒頭の識別子と、この grep コマンド例の2箇所に出る）
3. `date` を1行流して日付を照合する（★本セッションは2回日をまたいだ。⑩参照）

## ① プロジェクト棲み分け（混同禁止）

- 本件は **shunya-pms**（`~/shunya-production-system` / GitHub `shintarokoenuma/shunya-pms`）
- saagara-v2 / earnpulse / swtras-showroom は**別物**。コマンドを流す前に `pwd` を見る

## ② git の実態（2026-09-13 22:15 JST 実測）

- ブランチ **main**（docs を main に commit した日なので main のまま終える）
- HEAD **5b06f49**・origin と同期・未 push なし・untracked なし・作業ツリー クリーン・**open PR 0件**
- 本セッションの commit（**すべて docs のみ。src は1行も触っていない。DB も無触**）

| SHA | 内容 |
|---|---|
| eb4584a | B-202 仕様確認書 v1.0（197行）＋ BACKLOG に B-202 / B-203 を起票 |
| 5b06f49 | メモ受信箱に M-008〜M-010 を同期（62行追記・削除0）＋ B-054 / B-146 / B-203 の定義欄に追記（3行の置換・行数不変） |

## ③ 本セッションの成果 ― B-202 の設計が全部確定した

**品番カルテ詳細（`/products/[id]`）を1画面化する。** 3案の参考UIを見て案C（仕様書型）を採用し、
v1.0 の D-1〜D-8 と、addendum v0.1 の Q2〜Q11 まで確定した。

- 正本1: `docs/specs/b-202-product-karte-one-screen-spec-confirmation-v1_0-2026-09-12.md`（197行・識別子 RECON-L）
- 正本2: `docs/specs/b-202-spec-addendum-v0_1-2026-09-13.md`（★本セッションで作成・識別子 QLOG-B202）
- ★**2本セットで正**。v1.0 を単体で読むと Q6 / Q8 の訂正が反映されていない

### 確定の要約

| 面 | 決まったこと |
|---|---|
| 全体 | 1画面に7面（ヘッダー／絵型／品番・分類／縫製指示11項目／SKU 色×サイズ数量／進行／メモ）。BOM・見積・原価・伝票・書類は**下開きの引き出し** |
| 絵型 | **1枚＝1タブ・タブ名は `caption`**（面キーは足さない） |
| 進行 | ★**もともと直列ではなかった**（B-101 v1.0 の12行チェックリスト・SKIPPED も設計済み・状態は Badge で描き分け済み）。直すのは「直列の暗示をやめる」「1画面に収まる密度にする」の2点のみ |
| メモ | ★**休眠中の `Comment` を起こす**（ログ型・編集履歴あり）。`Product.internalNotes`（上書き型・実装済み）は併存で温存 |
| 表示スイッチ | Comment の使わない機能を隠す。**段階1（メモ欄の中だけ・会社で一括・`CompanySetting.uiPreferences`）**。人ごとは B-203 |
| 引き出し | 下開き。★同時に1つ・中身は内側でスクロール・横長の表は引き出し内で横スクロール |
| 受注 | 引き出しのまま |

### ★schema 変更は1本だけ

**`Comment` / `CommentMention` を起こす ADD TABLE 相当（非破壊）のみ。**
Q2・Q3・Q4・Q6・Q10 はすべて既存の器で足りた。

## ④ 次にやること（ライフサイクル位置つき）

ライフサイクルは **ステップ3（品番発番・品番カルテ）の横断UI**。原マイルストーンの M番号は無い（UI・情報設計）。
★手前の空きステップ: B-173 動線マップ（2026-08-29 実測）では **1・2・5 に画面が無い**。
ただし本件が使うのは既存の Product / Sku / ProgressTask / sketchImages で、**空きステップに依存しない**。

| 順 | 内容 | ステップ | 本番影響 | 不可逆性 | 規模 |
|---|---|---|---|---|---|
| 1 | 実装ブリーフの前の実測3点（下記⑤） | 3 | なし | なし | 小 |
| 2 | B-202 実装ブリーフ | 3 | なし | なし | 中 |
| 3 | `Comment` を起こす PR（schema＋validator＋action＋UI） | 3 | **あり** | ★migration の適用は不可逆 | 大 |
| 4 | 1画面化の本体（page.tsx の組み替え＋要約表示） | 3 | **あり** | あり | 大 |

★B-146（型紙の版）・B-054（PDF）・B-203（表示設定の基盤）は**別番号**。B-202 には含めない。

## ⑤ 実装ブリーフの前に必ず実測する3点

1. `Product.sketchImages` の `caption` の実データ（dev・read-only）＝タブ名に使えるか
2. migration 運用の3点（`npx prisma migrate status` 等）。★`prisma migrate dev` は使わない（⑨）
3. `CompanySetting` の dev 実データ（行が存在するか。★休眠テーブルなので「無ければ作る」処理が要る）

## ⑥ B-番号の増減

- **新規 2件**: **B-202**（品番カルテ1画面化）／**B-203**（表示項目の出し分けの共通基盤・段階2/3）
- **定義欄への追記 3件**: **B-054**（PDF の要件）／**B-146**（PatternVersion の実測）／**B-203**（段階の二層構造）
- **状態変更 0件** ／ **取り下げ 1件**（★B-204 の起票案。BACKLOG の grep で B-146 が受けていると判明し、起票せず撤回）
- **番号未採番の合意 0件**
- BACKLOG は **334行 / B行 203件 / 最大 B-203 / 番号重複なし**
  （★前メモ⑦の「BACKLOG は 201行」は件数を行数と書いた取り違え。正しくは件数）

## ⑦ 繰り延べた要件（3件・新規採番ゼロ）

| 要件 | 受け先 |
|---|---|
| PDF はヘッダー共通＋絵型主体＋出力ページ選択 | **B-054**（定義欄に追記済み） |
| 型紙の版管理（`PatternVersion` を起こす） | **B-146**（定義欄に追記済み） |
| 人ごとの表示設定・PDF の出力ページ選択の共通化 | **B-203**（定義欄に追記済み） |

★**すべて既存番号が受領。浮いている要件はゼロ。**

## ⑧ メモ受信箱（M-001〜M-010）

- ★正本はプロジェクトナレッジ側の `claude/MEMO_INBOX.md`。repo の `docs/MEMO_INBOX.md` は同期先
- 本セッションで **M-008 / M-009 / M-010** を両方に保存した（repo は 5b06f49・307行・`### M-` が10・62 insertions / **0 deletions**）
- M-008 参考UIの要望 ／ M-009 案C採用と追加4要望 ／ M-010 メモの器＝Comment・表示スイッチは段階1

## ⑨ migration 運用（変更なし・毎回確認する）

- `prisma migrate dev` は**使わない**（dev に `_prisma_migrations` が無く DB 全体の reset を要求する）
- 正運用: `migrate diff` のドライラン → `db push` → **手書き** migration → `migrate diff` の出力と手書きの一致検証 → 本番は Railway の `migrate deploy`
- ★`prisma migrate reset` と `--accept-data-loss` は**実行も提案もしない**
- dev DB は **hopper.proxy.rlwy.net:12921**。★本セッションは接続先を見ただけで**読み書きゼロ**

## ⑩ 日付について（★本セッションは2回日をまたいだ）

- 09-11 23:49 → 09-12 08:34（1回目）／09-13 05:48（2回目）
- spec v1.0 のファイル名 2026-09-12 は**設計確定日**。commit は 09-13（addendum の冒頭に注記した）
- ★長いセッションでは `date` を取り直す。本セッションでは6回取り直して毎回照合した

## ⑪ 本セッションの失敗と教訓

1. ★**モックで進行を直列（棒グラフ）に描いた。** 実装は12行のチェックリストで、行ごとの状態も SKIPPED も既にあった。
   **B-101 v1.0 を読む前にモックを描いたことが原因。** 慎太郎さんの「順序通りにならない」を受けて読み直し、訂正した
2. ★**「品番サマリー1枚 PDF」という呼び名を b-094 spec から引いたまま、BACKLOG の B-054 の定義文（縫製仕様書の帳票出力）と照合していなかった。**
   締めの重複確認で判明。同じ番号の別の言い方だった
3. ★**B-204 を起票しかけた。** BACKLOG を grep したら B-146 が受けていた。
   `shunya-session-start` の「起票前に grep」が効いた事例
4. ★**空の grep を「存在しない」の根拠にしなかったことで、誤った設計判断を避けられた。**
   進行の状態の描き分けは、別ファイルから import した定数で実装されていた（v1.0 §4 Q8 に「断定しない」と書いていた）
5. ★**同一出力の再提示が1回発生。** 鉄則8に従い分析せず、必要なブロックを出し直した
6. ★**通信エラーで保存ブロックが中断した。** 状態確認のブロックを先に流して「実行前・途中・完了」を切り分けてから再開した。
   STEP 0 の二重追記ガードが効いた

## ⑫ ナレッジ同期の状況（★隠さず書く）

| ファイル | 状態 |
|---|---|
| `claude/b-202-product-karte-one-screen-spec-confirmation-v1_0-2026-09-12.md` | 登録済み（repo eb4584a と同一本文） |
| `claude/b-202-spec-addendum-v0_1-2026-09-13.md` | ★**本締めで登録**（repo と同一本文） |
| `claude/b-202-open-questions-log-2026-09-13.md` | 登録済み（Q2〜Q11 の確定ログ・識別子 QLOG-B202）。★addendum に焼いたので**以後は addendum が正** |
| `claude/MEMO_INBOX.md` | 正本。M-001〜M-010 |
| `SESSION_HANDOVER.md` | ★**本締めで差し替え**（claude.ai 側の project_write で実施） |
| `BACKLOG.md` | ★**未同期のまま繰り越し。** ナレッジ側は **2026-09-09 スナップショット（196行）**、repo は **334行**。解消手順は `git show origin/main:docs/BACKLOG.md` の全文ダンプを1回取り、claude.ai 側で差し替えること |

★**次セッションで BACKLOG のナレッジ版を見るときは、B-197〜B-203 が載っていない古い版**であることを前提にする。

## ⑬ 更新したスキル（★次のチャットから有効）

- **`shunya-design-reread`** に2点を追記する提案カードを claude.ai 側で発行した
  1. ★**設計判断を仰ぐときは、文字の実測だけでなく参考UI（モックアップ）を添える**（M-008 の要望・本セッションの起点）
  2. ★**新しい器を作る前に、休眠モデルが受け皿になっていないかを当たる**（本セッションで Comment / CompanySetting / PatternVersion の3つが該当した）
- ★**実行環境のスキルはチャット開始時点のスナップショットで固定される。** 保存しても**このチャットには反映されない。次のチャットから有効**

## ⑭ 締めの状態

- ブランチは **main のまま**終えている
- 未マージ PR **なし**
- dev / 本番 DB は**本セッションで一切触っていない**（docs のみ・src 0行・migration 0本）
